{
  "name": "server",
  "version": "1.0.0",
  "description": "intro to backend folder structure",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "abiola",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  }
}
